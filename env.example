# =====================================================
# FRONTEND ENVIRONMENT VARIABLES (PUBLIC - SAFE TO EXPOSE)
# =====================================================

# Supabase Configuration
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Site URL for authentication redirects
# Should match your development/production domain
VITE_SITE_URL=http://localhost:3000

# =====================================================
# SECURITY NOTES
# =====================================================
# 
# ✅ SAFE to include in frontend (VITE_ prefix makes them public):
#    - VITE_SUPABASE_URL (your Supabase project URL)
#    - VITE_SUPABASE_ANON_KEY (public anon key - protected by RLS)
#    - VITE_SITE_URL (your site URL)
#
# ❌ NEVER include in frontend:
#    - Supabase SERVICE_ROLE_KEY (backend only!)
#    - OpenAI API keys (backend only!)
#    - Database passwords
#    - Any secret keys or tokens
#
# 🔒 How to protect sensitive data:
#    1. Store service_role key only in Supabase Edge Functions or backend
#    2. Use Row Level Security (RLS) to protect database access
#    3. Never commit .env.local files to git
#    4. Use Vercel environment variables for production secrets
#
# =====================================================
# DEVELOPMENT SETUP
# =====================================================
# 
# 1. Copy this file to .env.local
# 2. Fill in your actual values
# 3. For Supabase redirect URLs, add these to your Supabase Auth settings:
#    - http://localhost:5173/auth/callback (development)
#    - https://your-domain.vercel.app/auth/callback (production)
